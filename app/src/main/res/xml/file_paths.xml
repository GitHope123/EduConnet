<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Directorio cache para archivos temporales -->
    <cache-path name="cache" path="."/>

    <!-- Directorio de archivos externos (opcional) -->
    <external-path name="external" path="."/>

    <!-- Directorio de archivos externos públicos (opcional) -->
    <external-files-path name="external_files" path="."/>
</paths>